# NetSee 安全策略

NetSee 由两部分组成：本机 CLI 客户端和公网探针服务器（VPS）。探针是公开可达的观测端点，属于攻击面；客户端生成的报告包含用户出网环境的敏感事实。当前处于 Phase 0，尚无受支持版本。

## 报告漏洞

请优先使用仓库托管平台的私有漏洞报告功能。若该功能尚未启用，请通过维护者公开资料中声明的私有安全联系方式报告，不要创建包含可利用细节、隐藏测试数据或真实用户信息的公开 Issue。

报告应尽可能包含受影响版本或 commit、环境、复现步骤、影响、日志的脱敏摘要和建议缓解方式。不要在未经授权的系统上进行测试。

## 当前支持范围

| 版本 | 状态 |
|---|---|
| 无 | 尚未发布；不提供生产安全保证 |

## 安全原则

- 探针只记录会话内事实，不持久化任何客户端数据；会话注册表仅存内存，TTL 到期即清理。
- 探针不执行、不解释客户端载荷：echo 只回显原始字节或固定字段。
- TLS 嗅探只被动读取握手前 ClientHello；不解密、不记录后续流量。
- 会话 ID 使用 UUID v4，不可枚举；拉取观测必须关联会话。
- 报告测的是暴露面（沿途节点*能*看到什么），不是实际监视；结论必须标注前提与置信度，禁止制造虚假暴露感或虚假安全感。
- 安全结论必须关联威胁、控制、测试和环境证据。

完整模型见[威胁模型](docs/security/threat-model.md)。

## 响应原则

维护者确认问题后应限制信息传播、评估受影响范围、准备修复与回归测试、制定升级和缓解方案，并在用户可采取行动时发布公告。不得为了快速关闭报告而删除失败安全测试或降低既有控制。
